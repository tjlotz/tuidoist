# tuidoist
tuidoist is a terminal-first toolkit built on the Todoist API, combining a scriptable CLI with an interactive TUI for fast task management. It gives developers precise, keyboard-driven control to query, transform, and manage tasks without leaving the terminal.
